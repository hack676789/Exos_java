# Use postgres/example user/password credentials
version: '3.9'

services:
  # Base de données PostgreSQL
  #db:
   # image: postgres
    #restart: always
    #shm_size: 128mb
    #environment:
     # POSTGRES_USER: josue
      #POSTGRES_PASSWORD: passer
      #POSTGRES_DB: to_do
    #ports:
     # - 5432:5432

  # Interface d'administration pour PostgreSQL (Adminer)
  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080

   #Base de données MongoDB
  mongodb:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: josue
      MONGO_INITDB_ROOT_PASSWORD: passer
    ports:
      - 27017:27017

  #Interface d'administration pour MongoDB (Mongo Express)
  #mongo-express:
    #image: mongo-express
    #restart: always
    #environment:
      #ME_CONFIG_MONGODB_ADMINUSERNAME: josue
      #ME_CONFIG_MONGODB_ADMINPASSWORD: passer
      #ME_CONFIG_MONGODB_SERVER: mongodb
    #ports:
      #- 8082:8081
    #depends_on:
      #- mongodb
